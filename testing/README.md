# Backend testing

This folder contains focused unit and contract tests for backend middleware,
validators, utilities, pronunciation metadata, and review-queue ranking.

Run this folder only:

```sh
node --test testing/*.test.js
```

Run the complete backend suite (including the existing `test` folder):

```sh
npm test
```
